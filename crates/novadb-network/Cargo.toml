[package]
name = "novadb-network"
version = "0.1.0"
edition = "2024"

[dependencies]
novadb-protocol = { path = "../novadb-protocol" }
novadb-common = { path = "../novadb-common" }
novadb-server = { path = "../novadb-server" }
novadb-storage = { path = "../novadb-storage" }